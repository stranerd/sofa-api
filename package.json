{
	"name": "sofa-api",
	"private": true,
	"scripts": {
		"lint:fix": "eslint --fix services/*/src services/*/emails bin",
		"build": "pnpm -r build",
		"lint:staged": "lint-staged",
		"prepare": "husky"
	},
	"lint-staged": {
		"*": "eslint --fix"
	},
	"devDependencies": {
		"husky": "^9.0.11",
		"lint-staged": "^15.2.7"
	},
	"dependencies": {
		"@k11/eslint-config": "^0.1.6",
		"@k11/prettier-config": "^0.1.0",
		"eslint": "^9.24.0"
	}
}